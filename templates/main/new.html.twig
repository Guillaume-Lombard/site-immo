{% extends 'base.html.twig' %}

{% block title %} {{ parent() }} | New{% endblock %}

{% block body %}

    <h3 class="ms-5 mt-3">Ajouter une annonce de vente ou de location</h3>

    <div class="container mt-2 mb-10">
        <div class="formCreate">
            {{ form_start(propertyForm) }}
            {{ form_widget(propertyForm) }}
            <button class="btn btn-outline-success ms-3 mt-2">Publier</button>
            {{ form_end(propertyForm) }}
        </div>
    </div>
{% endblock %}


