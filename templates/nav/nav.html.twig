
<!-- Navbar for menu -->

<nav class="navbar navbar-light bg-light">
    <div class="container-fluid justify-content-start">
        <a class="btn btn-outline-success me-2" href="{{ path('main_home') }}">Accueil</a>
        <a class="btn btn-outline-secondary me-2"href="{{ path('main_me') }}">Qui suis-je ?</a>
        <a class="btn btn-outline-secondary me-2"href="{{ path('main_aboutus') }}">A propos</a>
    </div>
</nav>